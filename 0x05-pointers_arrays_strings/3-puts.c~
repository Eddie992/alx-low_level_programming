#include "main.h"
#include <stdio.h>
/**
 * _puts -puts a sting followed by a new line  to standard output
 *
 *@str- is the name of the string
 *
 *return zero
 */
void _puts(char *str)
{
char *str;
str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
puts(str);
return (0);
}
